# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client
# httpx 必须 < 0.27:notebooklm-py 依赖的 playwright 会拉新版 httpx,
# 但 fastapi 0.109 自带 starlette 0.35 用 TestClient(app) 旧签名
# 跟 httpx>=0.27 的 Client(app=...) 不兼容
httpx<0.27
requests==2.31.0

# Auth (P1 feature ready)
# NOTE: 使用 bcrypt 直连,不再走 passlib。passlib 1.7.4 在 bcrypt>=4.0 上
# 拿不到 __about__ 属性,密码哈希会 500。
PyJWT==2.8.0
bcrypt>=4.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Phase 5: NotebookLM 集成(notebooklm-py 走 subprocess,装在 VM / 系统级即可)
# pipx install "notebooklm-py[browser]"  # 推荐
# pip install "notebooklm-py[browser]"    # 也可
